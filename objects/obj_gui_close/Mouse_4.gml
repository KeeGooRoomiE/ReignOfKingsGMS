/// @description Insert description here
// You can write your code in this editor
if (global.Mlayer = menu_layer)
{
	instance_destroy(parent_id);
	instance_destroy(self);
	global.Mlayer=prev_layer;
}